<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"
        tools:ignore="ProtectedPermissions"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK"/>
    <uses-permission android:name="android.permission.VIBRATE"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Planexia">

        <!-- ===== AUTH FLOW ===== -->
        <activity
            android:name=".OnboardingActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".WelcomeActivity"  android:exported="false" />
        <activity android:name=".LoginActivity"    android:exported="false" />
        <activity android:name=".RegisterActivity" android:exported="false" />

        <!-- ===== APP PRINCIPALE ===== -->
        <activity android:name=".MainActivity"    android:exported="false" />
        <activity android:name=".PlanningActivity" android:exported="false" />

        <activity android:name=".ui.modules.ModulesActivity" />
        <activity android:name=".ui.modules.AddModuleActivity" />
        <activity android:name=".ui.objectives.ObjectivesActivity" />
        <activity android:name=".ui.objectives.AddObjectiveActivity" />
        <activity android:name=".ui.tasks.TasksActivity" />
        <activity android:name=".ui.tasks.ObjectiveDetailActivity" />
        <activity android:name=".ui.progression.ProgressionActivity" />
        <activity android:name=".ProfileActivity" android:exported="false" />

        <!-- ===== CHRONO ===== -->
        <activity
            android:name=".ChronoActivity"
            android:exported="false"
            android:launchMode="singleTop"/>

        <service
            android:name=".ChronoService"
            android:exported="false"/>

        <!-- Notifications -->
        <activity android:name=".notifications.NotificationsActivity" android:exported="false"/>
        <receiver android:name=".notifications.TaskReminderReceiver" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
            </intent-filter>
        </receiver>

    </application>

</manifest>