<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"
        tools:ignore="ProtectedPermissions"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK"/>
    <uses-permission android:name="android.permission.VIBRATE"/>

    <application
        android:name=".PlanexiaApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Planexia">

        <!-- Premier écran lancé au démarrage -->
        <activity
            android:name=".OnboardingActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Auth -->
        <activity android:name=".WelcomeActivity"  android:exported="false" android:windowSoftInputMode="adjustResize" />
        <activity android:name=".LoginActivity"    android:exported="false" android:windowSoftInputMode="adjustResize" />
        <activity android:name=".RegisterActivity" android:exported="false" android:windowSoftInputMode="adjustResize" />

        <!-- Profil + Paramètres -->
        <activity android:name=".ProfileActivity"    android:exported="false" />
        <activity android:name=".ParametresActivity" android:exported="false" />

        <!-- App principale -->
        <activity android:name=".PlanningActivity"                    android:exported="false" />
        <activity android:name=".ui.modules.ModulesActivity"          android:exported="false" />
        <activity android:name=".ui.modules.AddModuleActivity"        android:exported="false" />
        <activity android:name=".ui.objectives.ObjectivesActivity"    android:exported="false" />
        <activity android:name=".ui.objectives.AddObjectiveActivity"  android:exported="false" />
        <activity android:name=".ui.tasks.TasksActivity"              android:exported="false" />
        <activity android:name=".ui.tasks.ObjectiveDetailActivity"    android:exported="false" />
        <activity android:name=".ui.progression.ProgressionActivity"  android:exported="false" />

        <!-- Premium -->
        <activity android:name=".ui.premium.PremiumStatsActivity" android:exported="false" />
        <activity android:name=".ui.premium.HelpSupportActivity"  android:exported="false" />

        <!-- Chrono -->
        <activity android:name=".ChronoActivity" android:exported="false" android:launchMode="singleTop"/>
        <service  android:name=".ChronoService"  android:exported="false" android:foregroundServiceType="mediaPlayback"/>

        <!-- Notifications -->
        <activity android:name=".notifications.NotificationsActivity" android:exported="false"/>
        <receiver android:name=".notifications.TaskReminderReceiver" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
            </intent-filter>
        </receiver>

    </application>

</manifest>